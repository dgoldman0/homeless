# Markdown report: throat-supported shift rail

A warp drive begins with motion: a passenger region carried by a shaped transport field. A wormhole begins with infrastructure: a supported throat that already carries the burden of making distant regions adjacent. This paper develops the architectural question at their interface: can the throat carry warp-like shift as part of its own support system?

Garattini and Zatrimaylov provide the closest formal comparison point through the wormhole--warp-drive correspondence. Their analysis identifies the obstruction faced by localized Alcubierre bubbles in Morris--Thorne wormhole geometries. The active-rail construction follows the structured-background branch: shift, support, curvature, and packet service are organized together inside throat infrastructure.

The architecture assigns the transport job to the throat support envelope:

```math
\operatorname{supp}(\beta^l) \subseteq \operatorname{supp}(A,T).
```

The service sequence is:

```math
\text{prepare support} \rightarrow \text{carry packet} \rightarrow \text{catch packet} \rightarrow \text{fade shift} \rightarrow \text{relax throat} \rightarrow \text{reset}.
```

The passenger-facing diagnostic follows the packet worldtube:

```math
 g_{\mu\nu}\dot{x}^{\mu}_{\rm pkt}\dot{x}^{\nu}_{\rm pkt}<0.
```

The reduced diagnostic script evaluates stressed service branches at `V=10`, `lambda=6`. Caught branches preserve packet clearance in the reduced screen. No-catch and late-catch comparison branches enter packet-norm-positive regions. Radial null-speed and packet/support-edge bundle diagnostics remain finite for the active-rail branch in the tested reduced window and are treated as supplemental clearance checks rather than the main visual evidence.

Under source availability, the work raises confidence in an engineering-feasible branch because the burden separates into physical jobs: support-contained shift, packet catch/rematch, capacity shaping, support-edge containment, release timing, and reset. The resulting object is a packet-centered service through a throat-supported shift rail. The literature integration treats wormholes as supported geometry, warp drives as transport-shift language, prepared routes as infrastructure control, and Garattini--Zatrimaylov as the closest formal comparison for the localized-bubble obstruction.
