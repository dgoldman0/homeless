# Framework Alignment Notes

The screening framework decomposes a wormhole-support proposal into support, access, repayment, buffer, matching, transition, and source-realism functions. For the metric family

```math
ds^2=-N(l,t)^2dt^2+B(l,t)^2dl^2+R(l,t)^2d\Omega^2,
```

it assigns:

```math
B(l,t) \rightarrow \text{radial proper-length and support-dilution control},
```

```math
R(l,t) \rightarrow \text{areal access geometry and flare shaping},
```

```math
N(l,t) \rightarrow \text{redshift, timing, matching, and horizon control}.
```

The branch follows that structure. The `B`-only protocol established the radial support-dilution actuator and the clean ramp-scaling behavior. The source-history screen then identified the active areal flare as the next controlled subsystem, so the redesign activates `R` as the flare/access-state actuator.

The access rule for the branch is affirmative and simple: keep `R` frozen during the quiet hold, and use `R` during setup/reset to flatten and restore flare curvature. This assigns dynamic `R` activity to flare gating and keeps traversal inside a static access geometry.
